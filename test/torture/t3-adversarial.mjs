/**
 * T3 -- adversarial sequences. PLACEHOLDER.
 *
 * Filled in S3: resize churn (incl. 1x1, 8000x8000, shrink with a live melt
 * population -- SN-08), DPR churn, density ramps, wind sign flips, negative
 * gravity mid-run, tab-hidden dt spikes, call-order abuse, and run-to-saturation.
 * Registered now so the tier order is stable.
 */

export function run() {
    // Intentionally empty in S0.
}
