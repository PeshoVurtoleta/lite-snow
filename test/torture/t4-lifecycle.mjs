/**
 * T4 -- lifecycle and hostile input. PLACEHOLDER.
 *
 * Filled in S2: every method after destroy(), double destroy(), clear() after
 * destroy(), updateAndDraw(null,...), a ctx missing ellipse, a ctx whose arc
 * throws (SN-10), a Proxy ctx recording every set, spawn before any update, and
 * the hostile maxParticles corpus (SN-15). Registered now so the order is stable.
 */

export function run() {
    // Intentionally empty in S0.
}
