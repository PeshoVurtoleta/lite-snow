/**
 * T8 -- cross-package conformance. PLACEHOLDER.
 *
 * Filled in S4: the SN-19 detector (the inlined toCssOklch must be byte-identical
 * to @zakkster/lite-color's across a fixed 64-color corpus), shared-ctx
 * co-existence of two engines in one frame, and SoA layout conformance (the
 * twelve names, their element types and lengths). Registered now so the tier
 * order is stable.
 */

export function run() {
    // Intentionally empty in S0.
}
