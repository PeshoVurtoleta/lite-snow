/**
 * T2 -- the canvas-state contract (snow's aliasing matrix). PLACEHOLDER.
 *
 * Filled in S2/S3: globalAlpha restored on normal return and on a thrown draw
 * call (SN-10), fillStyle set once per frame, every beginPath paired with one
 * fill (SN-06), and the engine never calls clearRect / save / restore /
 * setTransform / scale. Registered now so the tier order is stable.
 */

export function run() {
    // Intentionally empty in S0.
}
